{
  "name": "bella-note",
  "private": true,
  "description": "Bella Note monorepo — FastAPI API (apps/api) + React UI (frontend)",
  "workspaces": ["frontend"],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "lint": "npm run lint -w frontend",
    "lint:fix": "npm run lint:fix -w frontend"
  },
  "engines": {
    "node": ">=20"
  }
}
